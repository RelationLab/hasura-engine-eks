apiVersion: v1
kind: Service
metadata:
  name: {{ include "hasura-engine.fullname" . }}
  labels:
    {{- include "hasura-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hasura-engine.selectorLabels" . | nindent 4 }}
