type AddressLabelActionResult {
  address: String!
  labels: [String!]
  contents: [String!]
}

type Query {
  address_label_action(
    base_address: String
    labels: [String!]
    mode: String!
    limit: Int
  ): [AddressLabelActionResult]
}
